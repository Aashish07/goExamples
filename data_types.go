package main

import "fmt"

//Only letters, numbers and underscore are allowed to be used and not start with number

func main() {
	var number uint16 = 260
	number = number + 5
	fmt.Println(number)
}
